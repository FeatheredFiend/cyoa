{% extends 'base.html.twig' %}

{% block title %}Paragraph Actions{% endblock %}

{% block body %}
<div class="container-fluid mt-3 mb-3">
  <p class="hidden" id="title">{{title}}</p>
  <p class="hidden" id="gamebook">{{gamebook}}</p>
  <p class="hidden" id="paragraph">{{paragraph}}</p> 
  {% if app.user != null %}
    <div class="row">
      <div class="col-sm-12 col-md-4">
      <a href="{{ path('paragraphaction_create', {'gamebook': gamebook, 'paragraph': paragraph}) }}"><button type="button" class="col-12 btn-primary d-grid gap-2 nav-link text-white">Create Paragraph Action</button></a>   
      {{ knp_pagination_filter(pagination, {'p.number': 'Paragraph', 'pa.text': 'Text', 'pac.name': 'Paragraph Action Category', 'pao.name': 'Paragraph Action Operator', 'pa.actionvalue': 'Paragraph Action Value', 'paa.name': 'Paragraph Action Attribute'}) }}
      {{ knp_pagination_render(pagination, null, {}, {
      'align': 'center',
      'size': 'small',
      'rounded': true,
      }) }}
      </div>
      <div class="col-sm-12 col-md-8 table-responsive">
        <table class="table table-striped table-light">
          <thead>
            <tr>
              <th>ID</th>
              <th>Gamebook</th>
              <th>Paragraph</th>
              <th>Text</th>
              <th>Paragraph Action Category</th>
              <th>Paragraph Action Operator</th>
              <th>Paragraph Action Value</th>
              <th>Paragraph Action Attribute</th>
            </tr>
          </thead>
          <tbody name="paragraphactionTable" id="paragraphactionTable">
            {% for paragraphaction in pagination %}
            {% set id = paragraphaction.id %}
              <tr>
                <td><a class="nav-link" href="{{ path('paragraphaction_edit', {'id': paragraphaction.id, 'gamebook': gamebook}) }}">{{ paragraphaction.id}}</a></td>
                <td><a class="nav-link gamebookTable" href="{{ path('gamebook_view', {'gamebook': gamebook}) }}"></a></td>
                <td><a class="nav-link" href="{{ path('paragraph_view', {'gamebook': gamebook}) }}">{{ paragraphaction.paragraph }}</a></td>
                <td>{{ paragraphaction.text }}</td>
                <td>{{ paragraphaction.paragraphactioncategory }}</td>
                <td>{{ paragraphaction.paragraphactionoperator }}</td>
                <td>{{ paragraphaction.actionvalue }}</td>
                <td>{{ paragraphaction.paragraphactionattribute }}</td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </div>
  {% endif %}           
</div>
{% endblock %}
