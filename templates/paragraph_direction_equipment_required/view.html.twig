{% extends 'base.html.twig' %}

{% block title %}Paragraph Direction Equipment Required{% endblock %}

{% block body %}
<div class="container-fluid mt-3 mb-3">
  <p class="hidden" id="title">{{title}}</p>
  <p class="hidden" id="gamebook">{{gamebook}}</p>
  <p class="hidden" id="paragraph">{{paragraph}}</p> 
  <p class="hidden" id="paragraphdirection">{{paragraphdirection}}</p> 
  {% if app.user != null %}
    <div class="row">
      <div class="col-sm-12 col-md-4" id="searchBox">
      <a class="text-decoration-none" href="{{ path('equipment_create', {'gamebook': gamebook, 'paragraph': paragraph}) }}"><button type="button" class="col-12 btn-primary d-grid gap-2 nav-link text-white">Create Equipment</button></a>
      <a class="text-decoration-none" href="{{ path('paragraphdirectionequipmentrequired_create', {'gamebook': gamebook, 'paragraph': paragraph, 'paragraphdirection': paragraphdirection}) }}"><button type="button" class="col-12 btn-primary d-grid gap-2 nav-link text-white">Create Paragraph Direction Equipment Required</button></a>   
      {{ knp_pagination_filter(pagination, {'paer.text': 'Paragraph Direction', 'e.name': 'Equipment'}) }}
      </div>
      <div class="col-sm-12 col-md-8 table-responsive" id="tableView">
      <a id="toggleSearch" class="nav-link text-white text-center paleRed">Search</a>
        <table class="table table-striped table-light">
          <thead>
            <tr>
              <th>Gamebook</th>
              <th>Paragraph</th>
              <th>Paragraph Direction</th>
              <th>Equipment</th>
              <th>Delete</th>
            </tr>
          </thead>
          <tbody name="paragraphdirectionequipmentrequiredTable" id="paragraphdirectionequipmentrequiredTable">
            {% for paragraphdirectionequipmentrequired in pagination %}
            {% set id = paragraphdirectionequipmentrequired.id %}
              <tr>
                <td><a class="nav-link gamebookTable" href="{{ path('gamebook_view', {'gamebook': gamebook}) }}">{{gamebook}}</a></td>
                <td><a class="nav-link" href="{{ path('paragraph_view', {'gamebook': gamebook, 'paragraph': paragraph}) }}">{{paragraph}}</a></td>
                <td><a class="nav-link" href="{{ path('paragraphdirection_view', {'gamebook': gamebook, 'paragraph': paragraph}) }}">{{ paragraphdirectionequipmentrequired.paragraphdirection.id }}</a></td>
                <td><a class="nav-link" href="{{ path('paragraphdirectionequipmentrequired_edit', {'gamebook': gamebook, 'paragraph': paragraph, 'paragraphdirection': paragraphdirection, 'id': paragraphdirectionequipmentrequired.id}) }}">{{ paragraphdirectionequipmentrequired.equipment }}</a></td>
                <td>
                  <form action="{{ path('generic_delete', {'entity': 'paragraphdirectionequipmentrequired', 'id': paragraphdirectionequipmentrequired.id}) }}" method="post" onsubmit="return confirm('Delete this? This cannot be undone.');">
                    <input type="hidden" name="redirect" value="{{ app.request.requestUri }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete_paragraphdirectionequipmentrequired_' ~ paragraphdirectionequipmentrequired.id) }}">
                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                  </form>
                </td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
        {{ knp_pagination_render(pagination, null, {}, {
        'align': 'center',
        'size': 'small',
        'rounded': true,
        }) }}
      </div>
    </div>
  {% endif %}           
</div>
{% endblock %}
