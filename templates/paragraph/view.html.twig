{% extends 'base.html.twig' %}

{% block title %}Paragraphs{% endblock %}

{% block body %}
<div class="container-fluid mt-3 mb-3">
  <p class="hidden" id="title">{{title}}</p> 
  {% if app.user != null %}
    <div class="row">
      <div class="col-sm-12 col-md-4">
      <a href="{{ path('paragraph_create') }}"><button type="button" class="col-12 btn-primary d-grid gap-2 nav-link text-white">Create Paragraph</button></a>   
      {{ knp_pagination_filter(pagination, {'a.name': 'Name'}) }}
      {{ knp_pagination_render(pagination, null, {}, {
      'align': 'center',
      'size': 'small',
      'rounded': true,
      }) }}
      </div>
      <div class="col-sm-12 col-md-8 table-responsive">
        <table class="table table-striped table-light">
          <thead>
            <tr>
              <th>ID</th>
              <th>Number</th>
              <th>Text</th>
            </tr>
          </thead>
          <tbody name="feedTable" id="feedTable">
            {% for paragraph in pagination %}
            {% set id = paragraph.id %}
              <tr>
                <td><a class="nav-link" href="{{ path('paragraph_edit', {'id': paragraph.id}) }}">{{ paragraph.id}}</a></td>
                <td>{{ paragraph.number }}</td>
                <td>{{ paragraph.text }}</td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </div>
  {% endif %}           
</div>
{% endblock %}
