{% extends 'base.html.twig' %}

{% block title %}Equipment Effect Attributes{% endblock %}

{% block body %}
<div class="container-fluid mt-3 mb-3">
  <p class="hidden" id="title">{{title}}</p> 
  {% if app.user != null %}
    <div class="row">
      <div class="col-sm-12 col-md-4">
      <a href="{{ path('equipmenteffectattribute_create') }}"><button type="button" class="col-12 btn-primary d-grid gap-2 nav-link text-white">Create Equipment Effect Attribute</button></a>   
      {{ knp_pagination_filter(pagination, {'eea.name': 'Name'}) }}
      {{ knp_pagination_render(pagination, null, {}, {
      'align': 'center',
      'size': 'small',
      'rounded': true,
      }) }}
      </div>
      <div class="col-sm-12 col-md-8 table-responsive">
        <table class="table table-striped table-light">
          <thead>
            <tr>
              <th>ID</th>
              <th>Name</th>
            </tr>
          </thead>
          <tbody name="equipmenteffectattributeTable" id="equipmenteffectattributeTable">
            {% for equipmenteffectattribute in pagination %}
            {% set id = equipmenteffectattribute.id %}
              <tr>
                <td><a class="nav-link" href="{{ path('equipmenteffectattribute_edit', {'id': equipmenteffectattribute.id}) }}">{{ equipmenteffectattribute.id}}</a></td>
                <td>{{ equipmenteffectattribute.name }}</td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </div>
  {% endif %}           
</div>
{% endblock %}
